{% set active = 'professional' %}
{% extends 'UserAdminBundle::layout.html.twig' %}

{% block panel %}

    <div class="span8">
        <div class="box">
        <div class="tab-header">
            Editar estilos del profesional
        </div>
        <form method="post" class="fill-up" {{ form_enctype(form) }}>
        <div class="padded">
        {{ form_widget(form) }}
        <hr>
            <input type="submit" class="btn btn-block btn-success" value="Guardar cambios">
            </div>
        </form>
        </div>
    </div>
{% endblock %}

{% block nav_secondary %}
    {% include 'UserAdminBundle:Professional:menu.html.twig' %}
{% endblock %}