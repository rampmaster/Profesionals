{% extends 'UserProfesionalBundle:Default:settings_layout.html.twig' %}
{% set secondary_active = 'plans' %}
{% block panel %}

<h5 class="widget-name"><i class="icon-picture"></i> Planes</h5>
    <div class="row-fluid">
        <div class="span12 wizard">

            <!-- General form elements -->
            <div class="widget row-fluid">

                <div class="well">
                <div class="span10 offset1">
                <h2>Planes y Pagos</h2>
                {% if plan_available %}
                        <p>Su plan premium está activo. Esta cuenta es gratuita para usted porque estamos en fase de pruebas.</p>
                    {% else %}
                    <a href="{{ path('profesional_buy_product', {'symbol': 'test_plan'}) }}">Comprar plan</a>

                    {% endif %}
                    
                </div><div style="clear:both"></div>
                </div>
            </div>
        </div>
    </div>


{% endblock %}

